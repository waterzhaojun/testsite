<section>
    <h2>
    {{ section.settings.title }}
    </h2>
  {%- for block in section.blocks -%}
    {% case block.type %}
      {%- when 'title' -%}
      <h2>{{ product.title}}</h2>
      {%- when 'description' -%}
      <div>{{ product.description }}</div>
  {% endfor %}
</section>

{% schema %}
  {
    "name": "Test Section",
    "settings": [
      {
        "id": "title",
        "type": "text",
        "label": "test section id",
        "default": "TtTTT"
      }
    ],
    "blocks": [
      {
        "name": "Section title",
        "type": "title"
      }, {
        "name": "Description",
        "type": "description"
      }
    ]
  }
{% endschema %}